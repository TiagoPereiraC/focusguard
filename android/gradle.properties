org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.newDsl=false
# Disabled: Kotlin incremental compilation breaks on Windows when the project
# is on a different drive (D:) than the Pub cache (C:) — cross-drive relative
# paths are not supported by RelocatableFileToPathConverter.
kotlin.incremental=false
